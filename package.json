{
  "name": "somedayguide",
  "version": "1.0.0",
  "description": "Jon and Linzy's guide to the world.",
  "main": "index.js",
  "scripts": {
    "server": "http-server public/",
    "start": "export MODE=production && npm run data && babel-node scripts/init.js",
    "search-index": "babel-node scripts/generateIndex.js",
    "cli": "babel-node scripts/cli.js",
    "blogs": "babel-node scripts/fetchblogs.js",
    "data": "babel-node scripts/initData.js && npm run blogs && npm run search-index",
    "watch": "npm start && sleep 2 && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "fetch": "^1.1.0",
    "marked": "^0.6.1",
    "mustache": "^3.1.0",
    "node-fetch": "^2.6.0",
    "preact": "^10.0.0",
    "preact-render-to-string": "^5.0.6"
  }
}
